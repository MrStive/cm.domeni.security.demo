spring:
  datasource:
    url: "jdbc:h2:mem:security_db"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      springframework:
        security=DEBUG:
domeni:
  security:
    jwt:
      secret-key: ${JWT_SECRET_KEY:mySecret1234}
      expiration-time: ${JWT_EXPIRATION-TIME:300000}
